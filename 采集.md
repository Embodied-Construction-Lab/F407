# STM32 模仿学习数据采集简版接口

## 1. 结论

目标接口要求 STM32 每 **50 ms（20 Hz）** 输出一帧复合遥测，本体状态统一达到**100 ms（10 Hz）** 新样本。没有新传感器数据时可以重复最近值，但必须保持
`sensor_seq` 不变，并设置`sensor_is_new=0`。

当前  `F407/actor_spc@95af8f5` 尚未达到该目标：遥测周期为 100 ms，液压缸编码器轮询周期为 200 ms，DWJ 角度轮询周期为 100 ms。

因此本任务同时要求把遥测升级到 20 Hz、把构成训练状态的液压缸编码器更新升级到稳定 10 Hz；如果硬件无法达到，必须保留各自独立序号和
采样时间，不能把重复值标成新样本。

STM32 只负责输出机器状态、底层控制量和安全状态。以下内容不由 STM32 采集：

- RGB 相机图像：Orin 采集；
- 人工摇杆/专家 Action：PC 读取手柄，Orin 在 UDP 输入端按本地时钟落盘；
- ACT 数据集、Bucket Tip、归一化 Observation：PC 离线生成。

## 2. STM32 必须输出的字段

所有字段名称带单位后缀，禁止依靠注释猜单位。

### 2.1 时序身份

| 字段 | 类型/单位 | 含义 |
|---|---|---|
| `schema_version` | 文本 | 固定为 `stm32_control_telemetry.v1` |
| `control_seq` | `uint32` | 每 20 Hz 控制周期加 1 |
| `control_stamp_ms` | `uint32/ms` | 当前控制周期的 STM32 单调时间 |
| `sensor_seq` | `uint32` | 每次获得一组新传感器数据时加 1 |
| `sensor_stamp_ms` | `uint32/ms` | 传感器实际采样时间，不是串口发送时间 |
| `sensor_is_new` | `0/1` | 本帧是否包含新的 10 Hz 传感器样本 |
| `command_rx_seq` | `uint32` | 最近收到的 Orin 命令序号 |
| `command_source_stamp_ms` | `uint32/ms` | Orin 命令帧中携带的源时间，仅用于追溯，不与 STM32 时间直接相减 |
| `command_received_stamp_ms` | `uint32/ms` | STM32 收到该命令的本地单调时间 |
| `command_age_ms` | `uint32/ms` | 当前时刻减去命令接收时刻 |

### 2.2 真机状态

| 字段 | 单位 | 含义 |
|---|---|---|
| `boom_pos_mm` | mm | 大臂液压缸位移 |
| `stick_pos_mm` | mm | 小臂液压缸位移 |
| `bucket_pos_mm` | mm | 铲斗液压缸位移 |
| `boom_vel_mmps` | mm/s | 大臂液压缸速度 |
| `stick_vel_mmps` | mm/s | 小臂液压缸速度 |
| `bucket_vel_mmps` | mm/s | 铲斗液压缸速度 |
| `boom_angle_deg` | deg | 大臂关节角 |
| `arm_angle_deg` | deg | 小臂关节角；`arm` 与动作中的 `stick` 是同一自由度 |
| `bucket_angle_deg` | deg | 铲斗关节角 |
| `swing_angle_deg` | deg | 上车回转角 |
| `swing_vel_degps` | deg/s | 上车回转角速度 |

### 2.3 当前控制目标和底层输出

| 字段 | 单位/范围 | 含义 |
|---|---|---|
| `boom_v_ref_mmps` | mm/s | 限位处理后真正送入 PID 的大臂目标速度 |
| `stick_v_ref_mmps` | mm/s | 限位处理后真正送入 PID 的小臂目标速度 |
| `bucket_v_ref_mmps` | mm/s | 限位处理后真正送入 PID 的铲斗目标速度 |
| `swing_v_ref_degps` | deg/s | 限位处理后真正送入 PID 的回转目标速度 |
| `pid_out_boom` | `[-1,1]` | 大臂速度 PID 输出 |
| `pid_out_stick` | `[-1,1]` | 小臂速度 PID 输出 |
| `pid_out_bucket` | `[-1,1]` | 铲斗速度 PID 输出 |
| `pid_out_swing` | `[-1,1]` | 回转速度 PID 输出 |
| `valve_boom_deg` | deg | 大臂比例阀最终目标角 |
| `valve_stick_deg` | deg | 小臂比例阀最终目标角 |
| `valve_bucket_deg` | deg | 铲斗比例阀最终目标角 |
| `swing_percent` | % | 回转执行器最终输出百分比 |
| `pump_percent` | % | 泵最终输出百分比 |
| `pwm_boom` | PCA9685 count | 实际写入大臂通道的 PWM |
| `pwm_stick` | PCA9685 count | 实际写入小臂通道的 PWM |
| `pwm_bucket` | PCA9685 count | 实际写入铲斗通道的 PWM |
| `pwm_swing` | PCA9685 count | 实际写入回转通道的 PWM |
| `pwm_pump` | PCA9685 count | 实际写入泵通道的 PWM |

### 2.4 模式与安全状态

| 字段 | 类型 | 含义 |
|---|---|---|
| `control_mode` | 枚举文本/整数 | `homing`、`manual`、`policy`、`safe_zero` 等 |
| `homing_complete` | `0/1` | Homing 是否完成 |
| `command_valid` | `0/1` | 当前 Orin 命令是否合法且可执行 |
| `command_timed_out` | `0/1` | 当前命令是否已超时并归零 |
| `control_enabled` | `0/1` | 底层是否允许执行运动 |
| `estop` | `0/1` | 急停状态 |
| `limit_mask` | bit mask | 哪个轴触发了位置/速度限位 |
| `rs485_ok` | `0/1` | 液压缸编码器通信状态 |
| `dwj_ok` | `0/1` | DWJ/ADS1115 大臂与铲斗角度传感器状态；小臂角来自液压缸编码器换算 |
| `imu_ok` | `0/1` | 回转/IMU 状态 |
| `fault_flags` | bit mask | 其他故障位；每一位必须有固定定义 |
| `dropped_command_frames` | `uint32` | 因队列满、乱序或解析失败丢弃的命令累计数 |

## 3. 推荐串口字段顺序

```text
schema_version,
control_seq,control_stamp_ms,
sensor_seq,sensor_stamp_ms,sensor_is_new,
command_rx_seq,command_source_stamp_ms,command_received_stamp_ms,command_age_ms,
boom_pos_mm,stick_pos_mm,bucket_pos_mm,
boom_vel_mmps,stick_vel_mmps,bucket_vel_mmps,
boom_angle_deg,arm_angle_deg,bucket_angle_deg,swing_angle_deg,swing_vel_degps,
boom_v_ref_mmps,stick_v_ref_mmps,bucket_v_ref_mmps,swing_v_ref_degps,
pid_out_boom,pid_out_stick,pid_out_bucket,pid_out_swing,
valve_boom_deg,valve_stick_deg,valve_bucket_deg,swing_percent,pump_percent,
pwm_boom,pwm_stick,pwm_bucket,pwm_swing,pwm_pump,
control_mode,homing_complete,command_valid,command_timed_out,
control_enabled,estop,limit_mask,rs485_ok,dwj_ok,imu_ok,
fault_flags,dropped_command_frames
```

当前 USART2 为 115200、遥测缓冲区仅 256 字节，不能直接假设可容纳这组 20 Hz ASCII字段。实现前必须计算最坏单帧长度并留出至少 30% 带宽余量，同时扩大缓冲区。若不满足，优先把 PC/Orin 与 STM32 同步升级到 460800，或改用带版本号、长度和校验的二进制帧；不能通过删除序号、时间戳、目标速度或安全状态来节省带宽。

## 4. Orin 必须采集的数据

### 4.1 当前代码事实与缺口

- 现有日志可用于临时验证遥操作链路，**不能直接作为正式 ACT 数据集**。正式采集必须由
   `excavator-orin-runtime` 新增一个 Episode Recorder，分别保存下列原始流；不要继续扩展
  Orin 根目录的历史脚本。

### 4.2 STM32 原始帧（Orin 每收到一帧立即记录）

建议保存为 `stm32_raw.jsonl` 或等价的列式文件：

| 字段 | 含义 |
|---|---|
| `episode_id` | 当前示教 Episode |
| `raw_frame_seq` | Orin 对每个串口输入帧递增的序号，包括解析失败帧 |
| `orin_receive_monotonic_ns` | 串口读取完成时的 `CLOCK_MONOTONIC`，是跨模态对齐的权威时间 |
| `orin_receive_wall_ns` | 仅用于人工查看日期和日志关联，不参与训练配对 |
| `raw_payload` | STM32 原始完整帧；不得只保留解析后的值 |
| `parse_ok`、`parse_error` | 解析是否成功及失败原因 |
| `telemetry.*` | 第 2、3 节中解析后的全部 STM32 字段和单位 |

原始帧频率跟随 STM32 复合遥测，目标为 20 Hz。即使状态无效、命令超时或急停，也必须落盘。

### 4.3 原始手柄包（Orin 每收到一个 UDP 包立即记录）

建议保存为 `joystick_raw.jsonl`。PC 发送端必须先从“文本+两位小数”升级为数值协议：

| 字段 | 含义 |
|---|---|
| `episode_id` | 当前示教 Episode |
| `joystick_sample_seq` | PC 每次采样递增的序号，用于发现 UDP 丢包/乱序 |
| `pc_sample_monotonic_ns` | PC 采样单调时间；只用于 PC 内部周期和丢包诊断 |
| `pc_sample_wall_ns` | 可选墙钟时间；只用于人工关联日志 |
| `orin_receive_monotonic_ns` | Orin 收到该 UDP 包的时间；与状态、相机属于同一 Orin 时钟域 |
| `source_addr` | UDP 来源地址 |
| `controller_1_id`、`controller_2_id` | 手柄 GUID/稳定设备标识；不能只记显示名称 |
| `x1,y1,z1,x2,y2,z2` | Pygame 原始浮点轴值，不舍入、不拼成文本 |
| `buttons` | 所有与使能、急停、Episode 标注有关的按钮位图/数组 |
| `deadman_pressed` | 当前是否由人工保持允许运动 |
| `mapping_id` | 轴到动作映射版本 |
| `calibration_id` | 中位、正负极限、死区标定版本 |

PC 和 Orin 的 `CLOCK_MONOTONIC` 不是同一时钟，不能直接相减。只要手柄仍连接 PC，训练对齐
就使用 `orin_receive_monotonic_ns`；若以后将 USB 手柄直接接到 Orin，则同时记录的 Orin 采样
单调时间可以替代网络接收时间。

原始手柄目标采集/发送频率为 20 Hz。当前 `doublestick_send.py` 是 10 Hz，只能用于联调，
正式采集前必须升级；ACT 最终仍按 10 Hz 状态生成训练样本。

### 4.4 权威专家 Action（Orin 每个有效手柄样本记录）

建议保存为 `expert_action.jsonl`。它与原始手柄分开，便于复现不同标定版本：

| 字段 | 单位/范围 | 含义 |
|---|---|---|
| `action_seq` | 递增整数 | Orin 专家动作序号 |
| `source_joystick_sample_seq` | 整数 | 对应的原始手柄样本 |
| `action_stamp_monotonic_ns` | ns | Orin 完成映射时的单调时间 |
| `action_boom` | `[-1,1]` | 大臂归一化目标速度 |
| `action_stick` | `[-1,1]` | 小臂归一化目标速度 |
| `action_bucket` | `[-1,1]` | 铲斗归一化目标速度 |
| `action_swing` | `[-1,1]` | 回转归一化目标速度 |
| `action_valid` | `0/1` | 手柄完整、标定有效且人工使能 |
| `mapping_id`、`calibration_id` | 文本 | 能重放出同一专家 Action 的版本标识 |

动作顺序固定为 `[boom, stick, bucket, swing]`。这里保存“轴映射、中心校准和死区处理后，物理速度缩放前”的值；PC 与 Orin 不取反。STM32 中的 `pid_out_*`、阀角、PWM、实际速度都不是 ACT 专家 Action。

### 4.5 实际发送给 STM32 的命令（Orin 每次串口写入记录）

建议保存为 `command_tx.jsonl`。它不是 ACT 标签，但用于确认标签最终如何驱动真机：

| 字段 | 含义 |
|---|---|
| `command_seq` | 与 STM32 `command_rx_seq` 对应的 Orin 命令序号 |
| `source_action_seq` | 对应的专家 Action；安全零命令没有专家来源时为空 |
| `command_tx_monotonic_ns` | Orin 调用串口写入时的单调时间 |
| `boom_v_ref_mps,stick_v_ref_mps,bucket_v_ref_mps,swing_v_ref_radps` | Orin 单位缩放后的物理速度命令 |
| `raw_serial_payload` | 实际写入 STM32 的完整字节/文本 |
| `command_kind` | `manual`、`policy`、`safe_zero`、`estop_zero` 等 |
| `write_ok,write_error` | 串口写入结果 |

专家 Action、物理速度命令和 STM32 限位后 PID 目标必须分别保存，三者不能混为同一个字段。

### 4.6 RGB 原始帧（Orin 相机采集线程）

建议图像保存到 `camera_front/`，索引保存到 `camera_front_timestamps.csv`：

| 字段 | 含义 |
|---|---|
| `camera_frame_seq` | 相机帧递增序号 |
| `camera_stamp_monotonic_ns` | Orin 获取完整图像时的单调时间；若驱动有硬件时间戳则另存 |
| `camera_hardware_stamp_ns` | 可选，相机驱动/硬件源时间，不能冒充 Orin 单调时间 |
| `image_path` | Episode 根目录下的相对路径 |
| `width,height,pixel_format` | 图像格式 |
| `exposure,gain` | 驱动可获得时记录，便于解释明暗变化 |
| `camera_id,calibration_id` | 设备、内参和外参版本 |

原始 RGB 建议 20–30 Hz，至少稳定覆盖 10 Hz 状态。掉帧时保留序号缺口，不复制上一张图。

### 4.7 Episode 元数据（每次完整人工挖掘一份）

`episode.json` 至少包含：

```text
episode_id, task, operator_id, start/end wall time,
dig_target_m, material_id, success, failure_reason, intervention,
STM32 firmware commit, Orin runtime commit, teleop sender commit,
URDF hash, Machine Profile hash, mapping_id, calibration_id,
valve_calibration_id, pump_setting, camera_id, camera_calibration_id
```

`success/failure_reason/intervention` 在 Episode 结束后由操作者填写；不能从“程序正常退出”自动推断挖掘成功。

## 5. 频率和时间规则

1. 控制环和复合遥测：20 Hz，每 50 ms 一帧。
2. 目标传感器新状态：10 Hz，每 100 ms 一组；当前液压缸编码器为 5 Hz，必须升级或如实标记。
3. 相同 `sensor_seq` 通常会出现在连续两个控制帧中，只有第一帧 `sensor_is_new=1`。
4. STM32 遥测中的 `*_stamp_ms` 使用 STM32 单调时钟，不使用日期时间。
5. Orin 时间和 STM32 时间属于不同设备时钟，不能直接相减。
6. 序号回绕使用 `uint32` 模运算处理；除正常回绕外必须严格递增。
7. 数据无效、命令超时或急停时仍要发送遥测，并明确状态；不能停止记录。
8. PC 墙钟、PC 单调时钟、Orin 墙钟、Orin 单调时钟和 STM32 tick 是五种不同时间语义；
   原始值全部保留，训练配对统一使用 Orin `CLOCK_MONOTONIC`。
9. 生成 10 Hz `steps.csv` 时，对每个新状态选择不晚于状态接收时刻的最新有效专家 Action
   （零阶保持），计算 `action_age_ms` 并写入转换报告；超过配置的新鲜度阈值时丢弃该训练
   样本，不能静默配对。原始时间戳保留在 `steps.csv`，因此年龄可以重复计算。

## 6. 开发完成后的最低验收

连续记录 10 分钟并满足：

- `control_seq` 无异常重复、倒序或跳变；周期中位数约 50 ms；
- `sensor_seq` 每约 100 ms 增加一次，每个新序号仅一次 `sensor_is_new=1`；
- 每个非零阀/PWM 输出都能对应到目标速度、PID 输出、控制模式和安全状态；
- 命令停止或超时后，目标速度、PID/阀输出最终明确归零；
- 串口无截断帧、字段错位或无法解析值；
- Orin 能根据 `schema_version` 拒绝未知格式，而不是按旧列顺序猜测；
- Orin 能从手柄序号报告 UDP 丢包/乱序，不再从 `formatted_data` 文本反向解析两位小数；
- 任取一个 `steps.csv` 样本，都能追溯到 STM32 原始帧、手柄原始包、权威专家 Action、
  Orin 串口命令和 RGB；
- Episode 结束后最终专家 Action 和 STM32 控制输出均有明确零命令记录。

## 7. 可直接交给代码 AI 的任务描述

```text
请在 STM32 固件中新增版本为 stm32_control_telemetry.v1 的 20 Hz 复合遥测。
每个 50 ms 控制周期输出一帧；目标本体状态以 10 Hz 更新，重复旧状态时保持
sensor_seq 不变并令 sensor_is_new=0。

当前代码基线是 F407/actor_spc@95af8f5：TELEMETRY_PERIOD_MS=100、
ENCODER485_POLL_PERIOD_MS=200、DWJ_POLL_PERIOD_MS=100、USART2=115200、
TELEMETRY_BUFFER_SIZE=256。请明确修改这些基线，不要假设当前已经满足目标频率。

必须输出四组信息：
1. control/sensor/command 的序号和 STM32 单调时间；
2. 三个液压缸的位置/速度、三个关节角、swing 角度/速度；
3. 限位后的四轴目标速度、四轴 PID 输出、比例阀/回转/泵目标和最终 PWM；
4. control_mode、homing、命令有效/超时、control_enabled、estop、limit_mask、
   rs485_ok、dwj_ok、imu_ok、fault_flags 和丢命令计数。

字段名和顺序严格采用本文第 2、3 节；位置用 mm，直线速度用 mm/s，角度用 deg，
角速度用 deg/s，PID 输出用 [-1,1]，百分比用 %，PWM 用 PCA9685 count。
sensor_stamp_ms 必须是实际采样时刻，不得使用串口发送时刻替代。

RGB、人工摇杆专家动作、episode_id 和 Orin 接收时间不在 STM32 实现，由 Orin 记录。
不要把 pid_out、阀角、PWM 或测得速度命名为 ACT action。

请同时提供：序列化/解析测试、序号递增测试、sensor_is_new 测试、命令超时归零测试，
并估算单帧最大字节数；若 115200 波特率余量不足，使用更高波特率或带版本号的二进制帧。
```